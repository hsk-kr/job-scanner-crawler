{
  "name": "job-scanner-lib",
  "version": "0.0.1",
  "description": "Job Scanner library to crawl job pages.",
  "main": "src/index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon",
    "start": "pnpm run build && node dist/index.js",
    "build": "rimraf ./build && tsc"
  },
  "keywords": [],
  "author": "hsk.coder@gmail.com",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^21.2.1"
  },
  "devDependencies": {
    "@types/puppeteer": "^7.0.4",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
